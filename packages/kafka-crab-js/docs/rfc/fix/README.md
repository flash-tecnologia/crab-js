# Open fixes

This directory contains fixes that are still open. Each RFC describes a reproducible
failure, the intended contract, and the evidence required before it can move to
`docs/rfc/implemented`.

| RFC                                                       | Related item | Subject                                                                   |
| --------------------------------------------------------- | ------------ | ------------------------------------------------------------------------- |
| [0011](../implemented/0011-deterministic-prefetch-drain/) | M01          | Deterministic drainage of prefetched batches (moved: all evidence agrees) |
